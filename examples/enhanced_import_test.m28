# Test enhanced module import with export control
(print "=== Testing Enhanced Module System ===")

# Just use the module name directly
(print "\nImporting enhanced_module and using it directly...")
(import "modules/enhanced_module")

# Access using the full name directly
(print "\nUsing get directly:")
(print "GREETING via get:" (get enhanced_module "GREETING"))

# Print the modules variable directly to see what we have
(print "\nLooking for module:")
(print "Modules directory listing:")
(dict.keys modules)