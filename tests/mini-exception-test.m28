# Minimal Exception Test Suite

(print "Starting minimal exception test suite")

# Basic try/except
(print "\nTest 1: Basic try/except")
(try
  (print "Raising an exception")
  (raise "Basic exception")
  (print "This shouldn't print")
  (except
    (print "Exception caught successfully")))

# Try with finally
(print "\nTest 2: Finally block")
(try
  (print "In try block")
  (finally
    (print "Finally block executed")))

# Empty dictionary at end to test fix
(def (get_empty_dict)
  {})

# Call function and discard result
(get_empty_dict)

# Create a dictionary directly
{}

(print "Minimal exception test suite completed")