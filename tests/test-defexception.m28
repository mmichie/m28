(print "Testing defexception special form")

(defexception CustomError Exception)
(print "Custom exception defined")

(def flag False)

(try
  (try
    (raise ValueError "Test error")
    (except ValueError
      (print "Caught ValueError")))
  (except
    (set! flag True)))

(print "Test complete")