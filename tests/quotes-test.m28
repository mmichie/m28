# Test handling of quoted keys in dictionaries

# Define dictionary with quoted keys
(= quoted_dict {'a': 1, 'b': 2})

# Access dictionary value
(= a_value (get quoted_dict "a"))

# Print results
(print "Dictionary:" quoted_dict)
(print "Value of 'a':" a_value)