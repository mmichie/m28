# Testing the object protocol

(print "=== Object Protocol Test ===")

# Simple dictionary tests
(= test-dict (dict "name" "test" "value" 42))
(print "Dictionary:" test-dict)

# Dot notation access
(print "test-dict.name:" (. test-dict name))
(print "test-dict.value:" (. test-dict value))

# Method calls
(= test-list [1 2 3 4 5])
(print "List:" test-list)
(print "Length:" (. test-list length))

# String tests
(= test-str "Hello world")
(print "String:" test-str)
(print "Upper:" (. test-str upper))

(print "=== Test complete ===")